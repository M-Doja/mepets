extends layout

block content
  .main.container.clearfix.row
      .col-md-8.col-md-offset-2
        h1.display-4
          img.avatar.img-circle.hidden-xs-down(src='/images/avatar.png', alt='avatar')
          | #{name}
        h2.favorite-book Favorite Beer
        p.beerType #{type} - #{favorite}
        
        div#button.col-md-2
          a.btn.btn-light.pull-md-right(href='add') Add Pet
        div
          ul#petList
            
      
                        
